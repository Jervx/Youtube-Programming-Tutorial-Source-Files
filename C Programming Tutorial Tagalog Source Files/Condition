#include <stdio.h>

int main(){

    int x = 5;
    
    if(x > 5){
        //this code will only execute if the value of x is greater than 5
        printf("The value of x is greater than 5");
    }else{
        printf("The vaue of x is less than 5");
    }

}
